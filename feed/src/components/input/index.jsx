import { Container } from "./style";

export function Input({placeholder, type, onChange}){
    return(
        <Container placeholder={placeholder} type={type} onChange={onChange}>



        </Container>
    )
}